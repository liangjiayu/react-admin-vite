import { createFromIconfontCN } from '@ant-design/icons';

/**
 * iconfont平台生成的代码地址，使用 Symbol 模式。
 * 项目自定义的图标添加都 我的项目中，然后生成 在线链接。
 * @See https://www.iconfont.cn/manage/index?manage_type=myprojects
 */
const IconFont = createFromIconfontCN({
  scriptUrl: '//at.alicdn.com/t/c/font_4973500_ims3nla04e.js',
});

export default IconFont;
